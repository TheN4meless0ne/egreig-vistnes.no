---
title: Test Document
publishedAt: 2026-08-01
summary: A sample document used to verify that getDocuments() and formatDate() work end to end.
---

This is a test document. It exists to confirm that `getDocuments()` correctly reads
frontmatter, and that `formatDate()` renders a sensible relative date next to it.

## Section heading

Some body content here — none of it is currently parsed as MDX/rendered HTML, since
there's no MDX compiler wired up yet (see below).
