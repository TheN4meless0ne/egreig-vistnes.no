export type FilterDims = {
    id: string;
    width: number;
    height: number;

    title: string;
    subtitle: string;
    heading: string;
    subheading: string;
    body: string;

    src: string;
    alt: string;

    name: string;
    destination: string;
}